# Changelog

All notable changes to VeriPost will be documented here.

## Unreleased

- Added bounded no-auth World App account-state init retries while account context is still missing: the runtime can resend the same SDK-compatible `command:"init"` payload up to five times on a one-second cooldown, including the case where native transport appears after an initial missing-transport pass, while still sending no messages on generic bridge-only pages.
- Deployed the bounded init-retry account-context build as `dpl_Dv2i5EAvwvJJYUUcEbcwPi1pqpwU`; full verification passed with 186 Vitest tests plus 7 GOALS, audit passed, and production probes confirm the root marker/header, healthy config/health, sparse JSON stale nonce 410, fake account-context proof rejection at Address Book verification, sparse World App stale auth recovery in-app, loaded external app assets free of MiniKit SDK/walletAuth/sign command markers, and redacted logs for page/stale-route/proof-rejection probes.
- Broadened the no-SDK World App account-state request so a real World App native bridge plus World App user-agent can receive the non-auth `command:"init"` request even before `window.WorldApp` is populated; generic bridge-only pages still receive no native message.
- Deployed the bridge-first account-context hydration build as `dpl_GbS3xhF7vMFmax3HQPx3BCvpshfT`; full verification passed with 184 Vitest tests plus 7 GOALS, audit passed, and production probes confirm the root marker/header, healthy config/health, sparse JSON stale nonce 410, fake account-context proof rejection at Address Book verification, stale auth recovery in-app, loaded external app assets free of MiniKit SDK/walletAuth/sign command markers, and redacted logs for page/stale-route/proof-rejection probes.
- Removed wallet-auth from the active Post path after the in-app authentication step still risked leaving World App: Post now refreshes logged-in World App account context, waits in place, and submits `{ draftText, worldAppAccount }` to `/api/proofs`.
- Deleted the same-origin `/api/world-wallet-auth/nonce` route and `src/lib/world-wallet-auth.ts`; stale wallet-auth nonce calls now return same-origin HTTP 410 `legacy_auth_removed`.
- Removed the remaining `@worldcoin/minikit-js` dependency and `MiniKit.install`; the runtime now reads existing World App/MiniKit/account-provider state and can send only a non-auth `command:"init"` state request when account context is missing.
- Deployed the no-SDK account-context build as `dpl_DiEvBPfskBtnHKfbBoFW6YwTfzT6`; full verification passed with 183 Vitest tests plus 7 GOALS, audit passed, and production probes confirm root marker/header, healthy config/health, no wallet-auth nonce route in the route list, sparse JSON stale nonce 410, fake account-context proof rejection at Address Book verification, stale auth recovery in-app, loaded external app assets free of MiniKit SDK/walletAuth/sign command markers, and redacted logs for probe rejections/traps.
- Uses logged-in World App account context plus backend World ID Address Book verification as the only production proof-creation path.
- Removed native IDKit proof creation, `@worldcoin/idkit-core`, `/api/world-proof/request`, World ID proof verifier helpers, RP signing config fields, native IDKit diagnostics, and the dormant client native-IDKit bridge API from the server/client contract.
- `/api/proofs` now accepts only `{ draftText, worldAppAccount }` account-context submissions; stale native proof and wallet-auth payloads fail closed as invalid requests.
- Deployed the hard-removal account-context build as `dpl_45YmLiZGST2cpQsmKG3XtnsCeUV1`; production probes confirm `/api/world-proof/request` is 404, stale native proof payloads return `invalid_request`, and the loaded assets no longer contain the old native IDKit bridge/request markers.
- Deployed the host-owned MiniKit state build as `dpl_GrFRetSBMejXoSRpYjPT5TY7pjex`; production probes confirm the loaded assets no longer contain the synthetic MiniKit bridge markers and still contain no wallet-auth, IDKit, connector, or X-login proof path.
- Deployed the pending-account Post build as `dpl_2TtsLrQoaxVtYvzAWC5MBX4WqcKA`; in a World App surface the Post action now starts an in-app account-context refresh/wait even when the account snapshot is not visible before the tap, with production probes confirming the new wait path and no restored auth, wallet, IDKit, connector, or native proof route markers.
- Deployed the static legacy-entrypoint recovery build as `dpl_DGPt99MShhagJJoXSfEjp1oGk3k5`; stale `/api/auth/signin/twitter` and `/world-miniapp` document requests now return a minimal same-origin recovery document with no `Location`, no middleware rewrite, no Next app-shell route state, and no OAuth/callback leakage.
- Restored a narrow SDK-compatible World App `init` state request when account context is missing, without importing MiniKit, creating/replacing `window.MiniKit`, or sending wallet-auth, sign, verify, connector, open-url, or redirect commands.
- The init-only runtime reports `world_app_state_init_sent` so production logs can distinguish "host state requested" from "account context detected" during the next real World App device test.
- Deployed the init-only host state request build as `dpl_6pbD3KUnMqGRs5vKxESzKUEPDBYn`; production probes confirm the loaded assets contain only the `init` command path, targeted MiniKit/IDKit/auth/sign/verify/connector/request markers are absent, stale auth recovery remains static/no-auto, and redacted logs include synthetic `world_app_state_init_sent`.
- Changed real World App stale document entrypoints such as `/api/auth/signin/twitter` and `/world-miniapp` from static recovery pages to internal compose-app rewrites, so a stale launch hydrates VeriPost in-place instead of stopping before the account-context flow can run.
- Deployed the World App stale-entrypoint app-rewrite build as `dpl_DDCzcJ8XqsG1ktiU171oMUW6JL41`; production probes confirm World App user-agent document requests to `/api/auth/signin/twitter` and `/world-miniapp` return compose-app HTML with `x-veripost-legacy-recovery: app-rewrite`, no `Location`, no static recovery page, no meta refresh, and non-World-App probes still receive static same-origin recovery.
- Removed the remaining early World App detection requirement from Post enablement; once config and text are valid, Post starts the same in-place World App account wait and can complete if the host injects account context after the tap.
- Deployed the late-detection Post wait build as `dpl_A5upJUSvDbdc3z23Qk9ngGUjwcpb`; production probes confirm the new root marker, late-wait UI markers in browser assets, World App stale-entrypoint app rewrites, non-World-App static recovery, fake account-context rejection at Address Book verification, and no restored MiniKit SDK, IDKit, wallet-auth call, connector, World universal-link, or old proof-request route markers.
- Restored official `@worldcoin/minikit-js` initialization only for `MiniKit.install(appId)` account-state hydration when the real browser `window.WorldApp` payload is present; the active Post path still does not call wallet-auth, sign, verify, connector, wallet permission, or World universal-link commands.
- Deployed the official MiniKit init-only build as `dpl_H937nugDGBtKG6Pp527d5iwLx1tW`; production probes confirm `MiniKit.install` and `init` state markers are present, World App stale-entrypoint app rewrites still stay in-app, non-World-App stale recovery remains static/no-auto, fake account-context proof still fails at Address Book verification, synthetic official-init diagnostics log `world_app_state_init_sent` with `stateContainer:"created"`, and no real-device proof-ready/proof-created logs have appeared yet.
- Deployed the startup MiniKit refresh and passive trigger-observer build as `dpl_81zbyPNa3FnoRAQWMrNBoL2wJ2HT`; the runtime now refreshes official MiniKit state from the current `window.WorldApp` payload at startup, passively captures account context from `MiniKit.trigger` payloads, ignores stale passive/MiniKit account state when the current World App surface changes, and still sends no auth/sign/verify/connector/redirect commands.
- Deployed the passive MiniKit response event build as `dpl_8QRa9cTKbMmkDnijwAaMTMMdrtnx`; the runtime now also listens for official `miniapp-*` response events, accepts World custom-scheme passive message origins only inside a World App surface, and preserves an early passive account payload when a walletless `window.WorldApp` payload appears immediately afterward.
- Deployed the early stale-entrypoint recovery build as `dpl_DVkFmusevLQ4nFWSbKGSSTgy16AT`; World App stale auth, mini app shell, root-query, and hash handoff URLs are normalized to `/` by the head-installed early guard before React hydration, and server app rewrites no longer target `/?legacy-auth=blocked` or `/?legacy-miniapp=rerouted`.
- Deployed the World App stale auth POST recovery build as `dpl_5FDJEZBpQZuJK83s7QipKvp9seHj`; no-query stale World App auth GET/POST launches now hydrate the compose app, while query-bearing stale auth/root handoffs use a same-origin `static-auto` recovery document that immediately returns to `/` without embedding callback/OAuth target values.
- Deployed the stale handoff copy/token cleanup build as `dpl_2Skx6U9C95JkGyEucWfGJtHBoFir`; stale recovery pages no longer use authentication wording, and root/app-shell HTML no longer ships raw stale handoff tokens such as `callbackUrl`, `redirect_uri`, `worldapp:`, or `worldcoin.org` while preserving the runtime guards.
- Deployed the sanitized World App static-fetch recovery build as `dpl_DqV43up7nsukgbpxyfHcq8xSHGGh`; query-bearing stale auth/root handoffs now return a same-origin loader that cleans the visible URL with `history.replaceState`, fetches the clean root app shell, avoids top-level `window.location.replace`, and does not serialize callback/OAuth target values into the response.
- Deployed the root-launch query evidence and auth-trigger recovery build as `dpl_HFUSN77Kpc8zcSPS6McVdufU19eF`; root requests now log redacted query shape, clean World-style `?path=/` launches are preserved and logged as safe, and bare/empty stale auth keys such as `?auth` and `?walletAuth=` recover in-app with query-source legacy auth diagnostics instead of leaving the mini app.
- Deployed the head-level World App runtime initial diagnostic build as `dpl_4B6X43qDCPm2bcVuEDa7LUbgtEt6`; likely World App launches now emit one redacted `world_runtime_initial` diagnostic before React hydration, focused tests passed with 115 tests, full verification passed with 207 Vitest tests plus 7 GOALS, and production probes/logs confirm the new root marker, clean `?path=/` handling, `?auth`/`?walletAuth=` recovery, stale nonce 410, fake account-context Address Book rejection, and one synthetic `world_runtime_initial` log with no wallet/draft payload leakage.
- Deployed the authenticate/authorize stale-alias recovery build as `dpl_9ADZTQZeAJ6UfiWD8YQdsLfGPniF`; `/authenticate`, `/authorization`, `/api/authorize`, `?authorize=true`, and `?path=/authenticate` now recover or fail closed inside the mini app across server, hydrated guard, head guard, compose stale-handoff recovery, cookie cleanup, and service-worker recovery, with focused auth-alias tests, full verification, audit, production probes, and redacted Vercel logs passing.
- Deployed the dashed sign-in/log-in/world-auth stale-alias recovery build as `dpl_EsifHZiSoGzi8PXnbn1tUxmouH2Y`; `/sign-in`, `/log_in`, `/api/sign-in`, `/api/log_in`, `/world/sign-in`, `/world-auth`, `?logIn=true`, `?sign_in=true`, and root path handoffs now recover or fail closed inside the mini app across server, hydrated guard, head guard, compose stale-handoff recovery, service-worker recovery, and redacted launch logging, with focused auth-alias tests, full verification, audit, Spectacula validation, production probes, and redacted Vercel logs passing.
- Deployed the passive-only World App account-context build as `dpl_4nbVpVT79qXCjN9QRVFy16PLopn1`; the active proof path no longer imports `@worldcoin/minikit-js`, calls `MiniKit.install`, sends raw `command:"init"` bridge messages, emits `world_app_state_init_sent`, mutates host `MiniKit`, or posts app-owned native bridge commands.
- Reinstated official `@worldcoin/minikit-js` initialization only for `MiniKit.install(appId)` account-state hydration when the real browser `window.WorldApp` payload is present, after official docs confirmed this is the supported in-app user-state setup and separate from wallet-auth/sign/IDKit prompt flows.
- Deployed the official MiniKit install-only account-context build as `dpl_7xuSakPzE3r9WnAtPB4B53zwmXxr`; full verification, audit, Spectacula validation, JSON validation, and diff checks passed, production probes confirm `MiniKit.install`/`command:"init"` markers plus continued absence of active wallet-auth/sign/verify/IDKit/connector/X-login/old proof-route call sites, stale-entrypoint recovery remains in-app/same-origin, fake account-context proof still fails at Address Book verification, and real-device proof-ready/proof-created logs remain pending.
- Narrowed the official MiniKit init path further: when the logged-in World App account is already exposed in `window.WorldApp`/MiniKit/account-provider state, VeriPost uses it directly and sends no MiniKit init; `MiniKit.install(appId)` is now only a missing-account fallback inside a real World App surface.
- Deployed the missing-account-only MiniKit init build as `dpl_9qVgFNETK9xTM15uryoRVeqFaa5R`; production probes confirm the new marker, healthy config/health, removed proof route 404, fake account-context rejection at Address Book verification, in-app/same-origin stale-entrypoint recovery, and no real-device proof-ready/proof-created logs yet.
- Removed the remaining `@worldcoin/minikit-js` dependency and official `MiniKit.install(appId)` fallback so the active proof runtime is passive-only again: it reads already exposed World App/MiniKit/account-provider state and passive account-context messages, but sends no native `command:"init"` and emits no `world_app_state_init_sent`.
- Deployed the passive-only no-init account-context build as `dpl_BAvFT4PsaJjgwR33vXPdCS3fjtCk`; production probes confirm the loaded assets contain account-context/wait/proof markers while `@worldcoin/minikit-js`, `MiniKit.install`, `command:"init"`, `world_app_state_init_sent`, active wallet-auth/sign/verify/IDKit/connector/X-login/old proof-route markers, and old native bridge senders are absent. Config/health are healthy, `/api/world-proof/request` is 404, fake account-context proof reaches Address Book rejection, stale document entrypoints recover in-app/same-origin, and real-device proof-ready/proof-created logs remain pending.
- Deployed the sparse World App launch recovery build as `dpl_5RFv7PMXgCAk9m3JtHRg2w1JgGhm`; World App stale auth and mini app shell requests with `Accept: */*` and `sec-fetch-mode: cors` now recover into the compose app with `x-veripost-legacy-recovery: app-rewrite` instead of returning JSON 410, while explicit JSON stale-auth calls still fail closed.
- Deployed the no-auto-navigation legacy recovery build as `dpl_DpQjgbNMMPBQrm8xB4GfzMKr1kxu`; stale `/api/auth/signin/twitter` and `/world-miniapp` document recovery now has no meta refresh, a strict `navigate-to 'self'; form-action 'none'` CSP, and only a same-origin continue link.
- Removed X login and separate auth routes from proof creation.
- Removed the primary `/api/proof-session/nonce` and `MiniKit.walletAuth` proof path so the Post tap uses the current account already exposed by World App instead of launching an authentication prompt.
- Added a stale `/api/proof-session/*` trap so older cached bundles fail closed with same-origin `410 legacy_auth_removed` instead of leaving the mini app.
- Hardened World App runtime detection so a native MiniKit transport or World App user-agent without an immediate raw account snapshot is treated as in-app and waits in place instead of opening the World mini app universal link.
- Removed the primary action's World App universal-link branch entirely; "Post" now stays on the page and waits for logged-in account context instead of navigating to `world.org/mini-app`.
- Replaced the post-proof X handoff with an explicit same-window anchor instead of app-owned `window.location.assign`.
- Added server and client traps for stale redirect-capable auth entrypoints so old `/api/auth`, `/api/world/*auth*`, wallet, sign, verify, OAuth, SIWE, and X-login paths stay in-app or fail closed with same-origin JSON.
- Narrowed the navigation CSP so browser-level external navigation is limited to explicit X/Twitter tweet-intent paths instead of broad X host navigation.
- Added client CacheStorage/service-worker cleanup plus self-unregistering `/sw.js` and `/service-worker.js` kill scripts so old redirect-capable WebView shells are forced off stale cached assets.
- Added immediate pre-hydration diagnostics from the early navigation guard so stale auth redirects blocked before React loads are visible in redacted production logs.
- Expanded stale auth traps to cover common MiniKit/SIWE sample endpoints such as `/api/nonce`, `/api/complete-siwe`, `/api/session`, `/api/verify`, and world-miniapp variants.
- Expanded stale IDKit/RP/signature/world-id shortcut traps such as `/api/idkit`, `/api/rp-signature`, `/api/sign`, `/api/world-id/*`, and world-miniapp variants so cached proof/auth bundles fail in-app.
- Hardened passive World App account-context capture for known World origins inside the World App surface and additional account wrapper/address aliases, without adding MiniKit install, wallet-auth, or native commands.
- Added direct `window.worldApp`, `window.world_app`, and `window.miniKit` account-state aliases so bridge casing variants can expose the logged-in account without starting auth.
- Added guarded `World` / `WorldID` / `worldId` / `worldcoin` direct and passive account-context aliases so host casing variants can expose the logged-in account when they carry World-account-shaped state, while unrelated globals are ignored.
- Added native transport detection aliases (`MiniKit`, `worldApp`, `ReactNativeWebView`, and Android casing variants), plus passive `WorldID`/`worldcoin` custom event names for account-context delivery.
- Expanded account-context parsing for nested wrapper profiles, `data`/`payload`/`miniApp` wrapper chains, `accounts`/`wallets` arrays, and root/nested MiniKit state account/version/device/verification state while preserving the no-auth bridge contract.
- Changed live blocked stale/external navigation attempts into an in-app notice instead of an error, so a stale auth event during the World account check does not stop proof creation.
- Removed the remaining synthetic `window.MiniKit` event bridge so VeriPost no longer shadows host-owned MiniKit state; it now only reads existing World App/MiniKit account context.
- Added a World App-surface-gated passive wallet provider fallback that can read an already exposed `window.ethereum.selectedAddress` or provider account state without calling `ethereum.request`, `eth_requestAccounts`, wallet permissions, wallet-auth, or sign commands.
- Expanded the passive wallet provider fallback to read already exposed official World EIP-1193 cached state such as `window.__worldapp_eip1193_address__`, provider account arrays, and account-address aliases while still never calling request/auth methods.
- Added document-level passive message listeners for Android-style WebView account-context delivery while keeping the same World App surface and origin gates.
- Forced the root compose route to render dynamically with no-store caching so World App webviews do not receive a prerendered redirect-capable app shell after deployment.
- Regenerated review screenshots from the no-redirect compose UI and removed stale X-login/demo proof-page screenshots.

## 0.1.0 - 2026-05-26

- Initial open-source release.
- Added mobile-first World Mini App compose flow.
- Added initial World ID proof flow.
- Added public proof pages and X Web Intent flow.
- Added World App Store submission documentation.
